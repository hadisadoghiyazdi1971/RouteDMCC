# Runtime environment

The original experiment source imports NumPy and Matplotlib in addition to Python standard-library modules. The minimal dependency list is provided only to make the source importable.

For the archival release, record the **actual versions used for the reported wall-clock experiments** rather than guessing them here. Runtime claims should be interpreted together with OS, CPU, thread count, and numerical-library backend.
